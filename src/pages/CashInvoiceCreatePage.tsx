import InvoiceCreateScreen from "@/screens/InvoiceCreateScreen";

/** فاتورة كاش (نقطة بيع) — إنشاء وتعديل. صفحة مستقلّة فوق الشاشة المشتركة. */
export default function CashInvoiceCreatePage() {
  return <InvoiceCreateScreen pos />;
}
