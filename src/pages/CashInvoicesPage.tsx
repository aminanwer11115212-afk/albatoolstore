import InvoicesScreen from "@/screens/InvoicesScreen";

/** إدارة فواتير الكاش — صفحة مستقلّة فوق الشاشة المشتركة. */
export default function CashInvoicesPage() {
  return <InvoicesScreen posOnly />;
}
