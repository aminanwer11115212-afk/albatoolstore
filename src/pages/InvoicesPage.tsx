import InvoicesScreen from "@/screens/InvoicesScreen";

/** إدارة الفواتير الآجلة — صفحة مستقلّة فوق الشاشة المشتركة. */
export default function InvoicesPage() {
  return <InvoicesScreen posOnly={false} />;
}
