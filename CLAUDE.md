# تعليمات العمل على هذا المستودع

## فتح الـPull Request — قاعدة دائمة

**بعد كل تعديل مكتمل: ادفع الفرع وافتح PR دون انتظار طلب من المستخدم.**

لا تسأل «هل أفتح PR؟» — افتحه. هذا إذن دائم من صاحب المستودع.

وإن كان على الفرع **PR مفتوح أصلاً**، فالكوميت الجديد ينضمّ إليه تلقائياً ولا
يُفتح غيره. أمّا إن كان الـPR السابق قد **دُمج**، فالعمل التالي يحتاج PR جديداً:

```bash
git fetch origin main
git rebase origin/main          # مع الاحتفاظ بالكوميتات غير المدموجة
git push --force-with-lease -u origin <branch>
```

ثم افتح PR جديداً. لا تُراكم كوميتات فوق تاريخ مدموج.

يحدث كثيراً أن يُدمج الـPR بسرعة، فتحقّق من حالته قبل افتراض أنه ما زال مفتوحاً.

## قبل كل دفع

لا تدفع قبل أن تنجح الثلاثة:

```bash
npx vitest run                          # كل الاختبارات
npx tsc -p tsconfig.app.json --noEmit   # لا أخطاء أنواع
npm run build                           # البناء ينجح
```

### ⚠️ `npx tsc --noEmit` وحده **لا يفحص شيئاً**

`tsconfig.json` ملفُّ حلٍّ لا مشروع: `"files": []` مع `references` فقط. فالأمر
المجرَّد يُصرِّف صفر ملف ويخرج ناجحاً دائماً — ولو كان في الكود اسمٌ غير معرَّف.

ثبت ذلك بالتجربة: ملفٌ فيه `undefinedIdentifierXyz()` يمرّ من `tsc --noEmit`
ويسقط من `tsc -p tsconfig.app.json --noEmit` بـ`TS2304`. واستدعاءٌ يتيم لدالّة
`setNotes` محذوفة عاش في `ChargeBalanceDialog` حتى انفجر عند المستخدم.

استعمل `-p tsconfig.app.json` دائماً، أو `npx tsc -b`.

## مهارتان تُطبَّقان على كل تعديل

في `.agents/skills/` مهاراتٌ لمواضيع المشروع. اثنتان منها ليستا لموضوعٍ بعينه
بل **لكل تعديل**، وهما الأهمّ:

- `albatool-fix-verification` — لا تعديلَ بلا عطلٍ مُعادِ إنتاجه: سطرٌ يُنتجه،
  وفحصٌ يسقط قبله وينجح بعده. والنمط يُبحث عنه في المشروع كلّه لا في موضعه
  وحده — فالعطل هنا يتكرّر لأن الحساب مكتوبٌ في مواضع متفرّقة.
- `albatool-integration-tests` — اختبار تكاملٍ بعد كل تعديل يقطع المسار كاملاً
  (شاشة ← دالّة ← إجماليات ← رصيد)، لا فحصَ دالّةٍ منعزلة. وكل اختبارٍ يغيّر
  عرضاً يُثبت أيضاً أن المجاميع لم تتحرّك.

## دوالّ الحافة لا تُعدَّل من هنا

سياسة لافابل ترفض تعديل `supabase/functions` من المستودع. فأيّ منطقٍ يُكتب هناك
**لا يصل الإنتاج أبداً** مهما دُمج — لا تكتب فيه، ولا تَعِد المستخدم بنشره.

ولهذا **رابط العميل لا يمرّ بدالّة حافة**: صفحة `/share/document/:token` تقرأ
بياناتها من دالّة القاعدة `get_shared_document` (حارسها التوكن) وتبني الورقة
بـ`generatePrintHTML` نفسها — راجع `src/utils/sharedDocumentHtml.ts`. فالتطابق
مع المعاينة **بنيوي لا تعاقدي**: قالبٌ واحد لا نسختان، وأي تعديل على الورقة
يصل العميل مع نشر الواجهة.

أما `supabase/functions/document-share` فمتروكةٌ مسارَ سقوطٍ للروابط القديمة
حتى تُطبَّق الهجرة، ولا تُطوَّر.

### قاعدة الورقة الواحدة

**أيّ تعديل على معاينة الفاتورة يظهر في رابط العميل، بشكل A4.**

فلا يُكتب تنسيقُ الورقة في صفحةٍ من الصفحتين: مكانه `generatePrintHTML`
وحدها (وما تحقنه فيه `printExtras`). ومقاسُ الورقة على الشاشة — 210mm
بهامش 10mm — في القالب كذلك، فيراه العميل كما يراه الـPDF.

ولهذا لا تُضاف قاعدة CSS في `DocumentPreviewPage` ولا في
`StandaloneShareDocument`: كلتاهما تعرض ناتج القالب في `iframe` بلا زيادة.
يحرس ذلك `src/test/printLayoutA4.test.ts`.

## قاعدة البيانات

هجرات Supabase **لا تُطبَّق من بيئة التطوير** — الوصول إلى `supabase.co` محجوب
بسياسة الوكيل، والمفتاح المتاح عام (anon) لا يملك تنفيذ DDL. أي هجرة جديدة
تحتاج تطبيقاً يدوياً من SQL Editor في لوحة Supabase، فنبّه المستخدم إليها
صراحةً في وصف الـPR وفي الرد.

## اصطلاح الإشارة في واجهة العميل

موحّد في النظام كلّه — الكشف، ورسائل الواتساب، والطباعة:

- `+` أخضر: في مصلحة العميل (رصيد له، أو خصم يُنقص دَينه)
- `−` أحمر: عليه
- الصفر بلا إشارة

المصدر الواحد لهذا التنسيق: `signedAmountText` في `src/utils/buildCustomerAccountView.ts`.

## رصيد العميل

الشحن **يُخزَّن ولا يُوزَّع**: لا يُسدَّد على أي فاتورة تلقائياً، ولا تتغيّر حالة
فاتورة إلا بسداد يدوي صريح من كشف الحساب. أي مسار جديد يكتب سداداً من الرصيد
يسقط اختبار العقد في `src/test/dataIntegrityLongterm.test.ts` — وهذا مقصود.
